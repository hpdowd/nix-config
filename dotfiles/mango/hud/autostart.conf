# Leaving noctalia mode. `stop` is idempotent and errors harmlessly when the
# unit is absent, which is what makes noctalia removable without touching this.
exec=systemctl --user stop noctalia 2>/dev/null; true
exec=pkill waybar 2>/dev/null; true
exec=pkill -x dsearch 2>/dev/null; true
exec=pkill -f '^swaync( |$)' 2>/dev/null; swaync &
exec=~/.config/mango/scripts/waybar/waybar-restart.sh
exec=~/.config/mango/scripts/system/gtk-apply.sh
exec=ln -sf ~/.config/mango/walker/configs/hud.toml ~/.config/mango/walker/config.toml
exec=pgrep '^\.?kdeconnectd' >/dev/null || kdeconnectd
exec=pgrep '^\.?elephant' >/dev/null || elephant
exec=~/.config/mango/scripts/walker/walker-daemon.sh
