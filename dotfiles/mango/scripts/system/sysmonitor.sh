#!/usr/bin/env bash
# `btm`, NOT dgop: dgop left the flake with DMS (docs/archive/MIGRATION.md), so
# this named a binary the machine does not have and the window died on open.
foot --app-id=sysmonitor -e btm
