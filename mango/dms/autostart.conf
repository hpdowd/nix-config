exec=pkill waybar 2>/dev/null; true
exec=pkill -x swaync 2>/dev/null; true
exec=pkill -x elephant 2>/dev/null; true
exec=pkill -f walker-daemon 2>/dev/null; true
exec=pgrep -x dms >/dev/null || dms run
exec=~/.config/mango/scripts/system/gtk-apply.sh dms
exec=pgrep -x dsearch >/dev/null || dsearch serve
